// Stub for the `server-only` package in the vitest node environment.
// The real package throws when imported outside a React Server Component
// environment; tests import server modules directly, so we neutralise it.
export {};
